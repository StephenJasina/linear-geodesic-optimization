A simple topology with two small clusters connected with a critical link. `complete` contains a network with the same nodes but all possible links.

Important note: `graph.json` does not conform to the format of other JSON blobs used to describe networks. It instead is a relic of some older scripts that would generate CSV files from JSON configurations. To use this example, use either the CSV files or the GraphML file.
