A simple topology with three clusters connected with two critical links. The clusters are arranged so that the overall topology is shaped like an elbow.

Important note: `elbow.json` does not conform to the format of other JSON blobs used to describe networks. It instead is a relic of some older scripts that would generate CSV files from JSON configurations. To use this example, use either the CSV files or the GraphML file.
